/** @import { Module } from "./index.js" */

/**
 * Build module to install NPM dependencies.
 * @param {Object} options
 * @param {string} options.path Path of the directory containing the package.json file.
 * @returns {Module}
 */
export function npmInstall(options) {
  return {}
}