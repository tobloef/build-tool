/**
 * Check if the directory at the given path exists and is indeed a directory.
 * @param file {string}
 * @return {Promise<boolean>}
 */
export function directoryExists(file: string): Promise<boolean>;
